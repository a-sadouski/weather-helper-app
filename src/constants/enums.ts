export enum Temperature {
    Celsius,
    Fahrenheit
}

export enum ThemeType {
    Optimal,
    Light,
    Dark
}

export enum Language {
    English = 'en',
    Russian = 'ru'
}

export enum StateActions {
    SET_CURRENT_WEATHER_DATA,
    SET_CURRENT_LOCATION_TITLE,
    SET_WEATHER_TEMPERATURE_TYPE
}

export enum AuthStoreActions {
    SET_LOGGED_IN_STATUS = 22
}
